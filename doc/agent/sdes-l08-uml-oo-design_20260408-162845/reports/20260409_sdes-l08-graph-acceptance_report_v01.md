---
Owner: Codex
Task: sdes-l08-graph-acceptance
Status: completed
Updated: 2026-04-09
---

# 软件设计师 L08 图依赖型二次验收报告

## 当前定位

- 当前课时：`L08 下午专题 III：UML / OO 设计`
- 当前阶段：图依赖型二次验收
- 结论：`L08 已达到基本通过门槛；“新需求改图”主线已建立，但权限语义与关系精度仍有可继续打磨处`

## 总评

- 严格口径总分：`17 / 21`
- 完全正确：`A.1 / A.2 / A.3`
- 基本通过：`B.1 / B.2 / B.3`
- 未达标：`无`
- 当前状态：`可将 L08 按基本通过处理，并按既定阶段策略进入阶段 B 真题 / 整题化回收`

## 逐问批改

### A.1

- 判定：完全正确
- 建议分：`2 / 2`
- 说明：
  - 你准确抓住了原模型失效的关键：
    - 原先默认只有 `User` 可以作为成员加入 `TopicGroup`
    - 现在 `TopicGroup` 本身也可以成为成员
  - 这正是新需求打破的核心假设

### A.2

- 判定：完全正确
- 建议分：`2 / 2`
- 说明：
  - 你给出的首选方案是：
    - 抽象 `Member` 层级
    - 让 `User` 与 `TopicGroup` 成为该层级下的具体类型
    - 再把 `joins` 关系落到 `Member` 层级
  - 这个思路非常稳，已经接近这类下午题的标准答案方向
  - 你还补充了“自关联”作为次优方案，也说明你已经开始比较不同建模方案，而不是只会盯着单一路径

### A.3

- 判定：完全正确
- 建议分：`2 / 2`
- 说明：
  - 你明确说出了：
    - 局部操作即使能在实现层跑通
    - 也不能在类图层面表达“专题组也能成为成员”的结构变化
  - 这正是本轮你最需要建立的意识：
    - 类图题先看结构表达，不先落到方法实现

### B.1

- 判定：基本通过
- 建议分：`4 / 5`
- 已答对：
  - 你正确识别了主要参与者：
    - `学员`
    - `讲师`
    - `班主任`
  - 你给出的主用例也基本成立：
    - `查看班级安排`
    - `发布资料作业`
    - `审核调班申请`
- 失分点：
  - 你提出：
    - “既然有审核，就应该也有提交调班申请”
  - 这个推断在业务上可以理解，但考试里不能随意补题干未给出的主用例
- 更标准口径：
  - 若说明文只明确写了“班主任可审核调班申请”，那本问就先按已给事实作答
  - 除非图或规约里另有证据，否则不要自行扩写“提交调班申请”

### B.2

- 判定：基本通过
- 建议分：`4 / 5`
- 已答对：
  - 你识别出的核心类基本合理：
    - `学员类`
    - `讲师类`
    - `班级类`
    - `提醒类`
    - `班级安排类`
  - 你还敏感地意识到了：
    - `调班申请类` 也可能成为候选类
  - 你正确指出了：
    - `班级类` 与 `班级安排类` 是组合关系
    - `学员类 / 讲师类` 与 `班级类` 存在关联
- 失分点：
  - “学员类、讲师类和班级类构成关联关系”这句还不够细
  - 更稳应拆开写：
    - `Student` 与 `ClassGroup` 的关联
    - `Instructor` 与 `ClassGroup` 的关联
  - 此外，`Notice` 虽然你答到了，但它与 `ClassGroup` 的触发或结果关系最好再点明

### B.3

- 判定：基本通过
- 建议分：`3 / 5`
- 已答对：
  - 你已经不再把题目答成“方法重载”
  - 你明确转向了：
    - 改 `关系`
    - 或改 `抽象层级`
  - 这是本轮最重要的进步
  - 你提出：
    - 给 `ClassGroup` 增加自关联
    - 或抽象一个 `Member` 层级
    这两个方向都比上一轮稳得多
- 失分点：
  - 你把：
    - `Member` 层级泛化为 `Student` 和 `ClassGroup`
    写出来了
  - 但题干还明确限定了权限差异：
    - 旁听班学员可查看主班安排
    - 但不能提交主班作业
    - 也不能替代主班班主任管理
  - 所以这题不只是“成员能不能嵌套”，还涉及“接入后的权限范围不是完全等同”
- 更标准口径：
  - 更稳的答法应补一句：
    - 原有模型不足，不只是因为班级之间要建立新关系
    - 还因为“旁听接入”对应的是受限访问关系，而不是完全等价成员关系
  - 因此改图时，除自关联或更高抽象层外，还应考虑如何表达这种受限权限语义

## 当前知识漏洞

1. 已经能把“结构改图”和“实现方法”分开，这是明显进步
2. 仍要继续收紧：
   - 关系的语义精度
   - “完全成员”和“受限访问”之间的区别
3. 对题干未明示的信息，仍要避免按业务常识自行扩写成正式答案

## 是否认为 L08 已通过

- 结论：`认为 L08 已基本通过`

### 理由

1. `L08` 的核心目标是建立：
   - 下午题固定模板
   - 新需求改图意识
2. 经过这轮二次验收：
   - 你已经能识别原模型失效的结构假设
   - 已能提出“改关系 / 改抽象层级”的答案
   - 也不再把题目答成单纯实现问题
3. 剩余问题主要是：
   - 语义精度
   - 权限边界表达
   这些更适合放到后续阶段真题 / 整题化回收里继续压实

## 下一步

1. 按已锁定的阶段策略：
   - `L08` 结束后，不直接进 `L09`
   - 先进入阶段 B 真题 / 整题化回收
2. 回收阶段继续重点观察：
   - 新需求改图
   - 关系语义精度
   - 受限权限与完全成员关系的区分
